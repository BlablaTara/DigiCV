export default function About() {
    return (
        <div>
            <h1>Om mig</h1>
        </div>
    );
}

// function About() = react komponent
// return() = HTML for show
// 'export default' = makes the component to a page